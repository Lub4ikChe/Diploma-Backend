/**
 * The Tokens object
 * @typedef {Object} Tokens
 * @property {string} accessToken - access token
 * @property {string} refreshToken - token for refreshing access token
 */
export interface Tokens {
  accessToken: string;
  refreshToken: string;
}
